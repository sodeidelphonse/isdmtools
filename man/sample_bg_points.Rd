% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BackgroundPoints.R
\name{sample_bg_points}
\alias{sample_bg_points}
\title{Generate background points}
\usage{
sample_bg_points(
  mask,
  points = NULL,
  n = 500,
  method = "random",
  values = FALSE,
  cells = FALSE,
  xy = TRUE,
  as.points = FALSE,
  ...
)
}
\arguments{
\item{mask}{A raster object (e.g. \code{spatRaster}) to be used as mask (preferably, the predicted intensity or habitat suitability).}

\item{points}{Spatial points (\code{data.frame}, \code{sf} or \code{SpatVector} objects) that would be excluded from the background sample.}

\item{n}{An integer specifying the number of pseudo-absence points to sample for presence-only data. Default is 1000.}

\item{method}{The sampling technique to select pixels from the raster mask (see \link[terra]{spatSample}), defaulted to \code{random}.}

\item{values}{A logical value indicating whether sampled cells values will be returned or not.}

\item{cells}{A logical value indicating whether sampled cells numbers will be returned.}

\item{xy}{A logical value indicating whether the locations of sampled cells will be returned.}

\item{as.points}{A logical value indicating whether spatial points object will be returned.}

\item{...}{Additional arguments passed to \link[terra]{spatSample}.}
}
\value{
A list object containing a \code{spatRaster} object (with \code{points} excluded if not \code{NULL}) and the background points.
}
\description{
A constructor function to generate background points for computing evaluation scores for presence-only data.
It exclude NA cells from the sample, and eventually the observed locations if needed.

Inputs:
}
\examples{
\dontrun{
library(terra)
set.seed(123)
r  <- rast(nrows = 100, ncols = 100, xmin = 0, xmax = 10, ymin = 0, ymax = 10)
terra::values(r) <- runif(ncell(r))
pts <- spatSample(r, size = 100, xy = TRUE, values = FALSE)

# Requesting few points with their x and y coordinates
set.seed(235)
bg_sample1 <- sample_bg_points(r, points = pts, n = 500, xy = TRUE, cells = FALSE)
plot(bg_sample1)
print(bg_sample1)

# Requesting points more than available non-NA cells
bg_sample2 <- sample_bg_points(r, points = pts, n = 10000, xy =TRUE, cells = FALSE)
dim(bg_sample2$bg)
plot(bg_sample2)
}
}
\seealso{
Other bg_points: 
\code{\link{plot.BackgroundPoints}()},
\code{\link{print.BackgroundPoints}()}
}
\concept{bg_points}
